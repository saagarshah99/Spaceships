{
  "homepage": "http://saagarshah99.github.io/Spaceships",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3"
  },
  "dependencies": {
    "build": "^0.1.4"
  }
}
